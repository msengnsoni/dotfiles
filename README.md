# dotfiles 設定ファイルまとめ

* .vimrc
$HOME/.vimrc

* .gitconfig
$HOME/.gitconfig

* .zshrc
$HOME/.zshrc

* com.googlecode.iterm2.plist
$HOME/dotfiles/ に置く

* Brewfile
brew bundle でインストール

* karabiner.json
$HOME/.config/karabiner/karabiner.json